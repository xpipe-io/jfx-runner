source("jfx") {
    git("https://github.com/openjdk/jfx/", "master")

    buildProperties("COMPILE_WEBKIT=false")
}

source("prUrl") {
    prUrl("https://github.com/openjdk/jfx/pull/2145")
}

source("prId") {
    prId(2146)
}

source("local") {
    local(file("C:\\Projects\\jfx"))
}
